# Git LFS would be needed for large model files
# For now, these files are excluded via .gitignore

# Large model files (excluded)
SAM3/model.safetensors filter=lfs diff=lfs merge=lfs -text
SAM3/sam3.pt filter=lfs diff=lfs merge=lfs -text
